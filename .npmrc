# next-auth@4 declares an optional peer on nodemailer@^7, but we run nodemailer@^9
# (to clear high-severity advisories) and do not use next-auth's email provider.
# legacy-peer-deps lets `npm ci` install per the lockfile without failing on that
# cosmetic optional-peer mismatch.
legacy-peer-deps=true
